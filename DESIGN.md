# QDII Watch — 系统设计文档

> 每日自动监测美股 QDII 基金的申购限额/额度状态，移动端优先的极简 Web 应用。
> 核心原则：**零运维、零成本（全免费额度）、自动化**。

---

## 1. 问题与目标

QDII 基金受外汇额度限制，申购状态频繁变化（正常开放 → 限大额 → 暂停申购），且限额数字（如"单日累计 1000 元"）会调整。手动逐个查询费时易漏。

**目标：**

- 每日自动抓取自选 QDII 基金的申购状态与限额
- 状态变化时主动推送通知（开放了 / 限额提高了 / 暂停了）
- 一个手机上随时可看的极简看板，含历史变化记录

**非目标：** 净值分析、收益计算、交易功能。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────┐
│                  Vercel（免费版）                  │
│                                                 │
│  Next.js App Router                             │
│  ├─ 前端页面（看板 / 详情 / 管理）                  │
│  ├─ Route Handlers（API）                        │
│  └─ /api/cron/fetch  ←── Vercel Cron（每日定时）   │
└──────────┬──────────────────────┬───────────────┘
           │                      │
   ┌───────▼────────┐    ┌────────▼────────┐
   │ Supabase        │    │ 数据源（抓取）     │
   │ Postgres（免费） │    │ 天天基金 F10      │
   └───────┬────────┘    │ 基金公司公告       │
           │              └─────────────────┘
   ┌───────▼────────┐
   │ 通知渠道         │
   │ Bark / Server酱 │
   │ / 邮件(Resend)  │
   └────────────────┘
```

**为什么这样选：**

| 决策 | 选择 | 理由 |
|---|---|---|
| 部署 | Vercel 免费版 | 与 Next.js 零配置集成，自带 Cron |
| 定时任务 | Vercel Cron（备选 GitHub Actions） | 免费、无需服务器；GHA 可作冗余触发 |
| 数据库 | Supabase Postgres 免费版 | 免费 500MB 足够（每日几十条记录），自带 REST/Auth 备用 |
| 通知 | Bark（iOS）/ Server酱（微信） | 免费、一个 HTTP 请求即可发推送 |
| 数据源 | 天天基金 F10「申购赎回状态」页 | 公开、结构稳定、包含申购状态与限额金额 |

---

## 3. 数据采集（核心）

### 3.1 数据源

主源：天天基金 F10 申购状态页（`fundf10.eastmoney.com`），可解析出：

- **申购状态**：开放申购 / 限制大额申购 / 暂停申购
- **单日累计申购限额**：如 `1,000 元`、`不限`
- 赎回状态（顺带记录）

备源（可选 v2）：基金公司公告页，用于交叉验证。

### 3.2 采集流程

```
Vercel Cron（每日 09:30、15:30 各一次）
  → /api/cron/fetch（校验 CRON_SECRET）
    → 遍历自选基金列表（串行 + 随机间隔，礼貌抓取）
      → 解析 状态 + 限额
      → 与库中最新快照对比
        ├─ 无变化 → 仅更新 checked_at
        └─ 有变化 → 写入新快照 + 触发通知
```

要点：

- **只存变化**（快照式存储），数据量极小，免费库用不完
- 抓取失败重试 2 次，仍失败则记录异常并在看板上标"数据过期"
- 监控基金数量建议 ≤ 30 只，单次执行控制在 Vercel 函数时限内

### 3.3 通知规则

仅在**状态或限额发生变化**时推送，避免噪音：

- 🟢 暂停 → 开放 / 限额提高：高优先级（抢额度窗口）
- 🔴 开放 → 暂停 / 限额降低：普通提醒
- 推送内容：基金名 + 旧状态 → 新状态 + 限额数字

---

## 4. 数据模型

仅 3 张表，刻意保持最小：

```
funds              自选基金
├─ code           基金代码（主键，如 270042）
├─ name           基金名称
├─ enabled        是否监控
└─ sort_order     看板排序

quota_snapshots    额度快照（仅变化时写入）
├─ id
├─ fund_code      → funds.code
├─ purchase_status   open | limited | suspended
├─ daily_limit       单日限额（分，null = 不限）
├─ redeem_status
└─ recorded_at

fetch_logs         抓取日志（排障用，保留 30 天）
├─ id, run_at, success_count, fail_count, error_detail
```

---

## 5. 前端设计

### 5.1 页面结构（共 3 页）

| 路由 | 内容 |
|---|---|
| `/` | **看板**：基金卡片列表，状态色点 + 限额数字 + 距上次变化时长 |
| `/fund/[code]` | **详情**：当前状态大字展示 + 历史变化时间线 |
| `/settings` | **管理**：增删基金、排序、通知开关（简单口令保护） |

### 5.2 视觉风格

**黑白灰极简，Linear / shadcn 质感：**

- 配色：纯黑 `#09090b` 背景（暗色优先）/ 纯白卡片（亮色），文字 zinc 灰阶；**唯一彩色是状态点**——绿（开放）/ 黄（限额）/ 红（暂停）
- 字体：`Geist Sans` + 数字用 `Geist Mono`（限额金额等宽显示，高级感关键）
- 组件：shadcn/ui（Card、Badge、Skeleton、Sheet、Sonner toast）
- 细节：1px 边框替代阴影、大量留白、12px 圆角、微妙的 hover 过渡（150ms）
- 移动端优先：单列卡片流，底部安全区适配，PWA manifest（可添加到主屏幕）

### 5.3 看板卡片示意

```
┌────────────────────────────────┐
│ 270042  广发纳斯达克100          │
│                                │
│ ● 限制大额        ¥ 1,000 /日   │
│ 3 天前由「暂停申购」变更           │
└────────────────────────────────┘
```

---

## 6. 技术栈清单

| 层 | 技术 |
|---|---|
| 框架 | Next.js 15（App Router、RSC 为主、Server Actions） |
| UI | Tailwind CSS 4 + shadcn/ui + Geist 字体 |
| 数据库 | Supabase Postgres + Drizzle ORM |
| 抓取 | Route Handler 内 `fetch` + `cheerio` 解析 |
| 定时 | Vercel Cron（`vercel.json` 配置）+ GitHub Actions 冗余 |
| 通知 | Bark / Server酱（HTTP 调用，渠道抽象成接口） |
| 部署 | Vercel 免费版，环境变量管理密钥 |

**月成本：¥0**（全部在免费额度内）。

---

## 7. 风险与对策

| 风险 | 对策 |
|---|---|
| 数据源页面改版 | 解析逻辑独立成单一模块；解析失败即告警，不写脏数据 |
| 抓取被限流 | 频率极低（每日 2 次、串行带间隔）；UA 伪装常规浏览器；备源切换 |
| Vercel Cron 免费版精度有限 | 对本场景（每日级）足够；GHA schedule 作为兜底触发 |
| 接口被恶意调用 | Cron 接口校验 `CRON_SECRET`；管理页简单口令 |

---

## 8. 实施路线

1. **MVP（1~2 天）**：抓取解析模块 + 数据库 + Cron + Bark 通知 —— 先解决"自动监测"核心痛点，没有界面也能用
2. **看板（1~2 天）**：首页卡片流 + 详情时间线 + PWA
3. **打磨（按需）**：管理页、暗色模式切换、备源交叉验证、限额变化趋势图
